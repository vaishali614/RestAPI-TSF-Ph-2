<% include partials/header %>

<div class="container">
    <div>
        <h1 style="text-align: center; color: white; margin: 5% 0;">VIEW ALL STUDENTS</h1>
        <div style="width: 80%; margin: 20px auto;">
            <table border="1 solid black" style="width: 100%; background: white;">
                <tr style="color: black; text-align: center;">
                    <th>NAME</th>
                    <th>AGE</th>
                    <th>EMAIL</th>
                    <th>CONTACT NO.</th>
                </tr>
                <% students.forEach(function(student) { %>
                <tr style="color: black; text-align: center;">
                    <td><%= student.name %></td>
                    <td><%= student.age %></td>
                    <td><%= student.email %></td>
                    <td><%= student.contact %></td>
                    <th><a href="/view/<%= student._id %>" class="btn btn-md btn-primary">VIEW <%= student.name %></a></th>
                </tr>
                <% }); %>
            </table>
            <br>
            <a href="/" class="btn btn-sm btn-success">Go Back</a>
        </div>
    </div>
</div>

<% include partials/footer %>